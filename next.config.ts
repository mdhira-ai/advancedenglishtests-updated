import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  reactStrictMode: false,
  images: {
  domains: ["d2cy8nxnsajz6t.cloudfront.net", "lh3.googleusercontent.com", "ui-avatars.com"],
  },

};

export default nextConfig;
